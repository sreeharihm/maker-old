import { AppResizeDirective } from './app-resize.directive';

describe('AppResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new AppResizeDirective();
    expect(directive).toBeTruthy();
  });
});
